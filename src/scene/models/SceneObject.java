package scene.models;

import raytracer.ray.Intersection;
import raytracer.ray.Ray;

public class SceneObject implements Hittable {
    @Override
    public Intersection hit(Ray r) {
        return null;
    }
}